{
  "name": "events-anth",
  "version": "0.1.0",
  "description": "Events discovery web app",
  "main": "server.js",
  "scripts": {
    "build:config": "node scripts/build-config.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "start": "npm run build:config && node server.js",
    "dev": "npm run build:config && node server.js"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "jsdom": "^29.0.2"
  },
  "license": "MIT"
}
